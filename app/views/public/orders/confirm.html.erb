<h1>注文情報の確認画面</h1>

<div class ="container">
  <div class ="row">
    <div class ="col-7">
      <h5 class="mt-3">注文情報確認</h5>
      <table class="table table-bordered">
        <!--tableタグはclassにtableをつけないとBootstrapが機能しない-->
        <thead>
          <tr>
            <th>商品名</th>
            <th>単価(税込)</th>
            <th>数量</th>
            <th>小計</th>
          </tr>
        </thead>
        <tbody>
          <% @cart_items.each do |cart_item| %>
          <tr>
            <td><%= cart_item.item.name %></td>
            <td><%= cart_item.item.price %></td>
            <td><%= cart_item.quantity %></td>
            <td><%= cart_item.item.price * cart_item.quantity %></td>
          </tr>
          <% end %>
        </tbody>
      </table>

    </div>

    <div class="col-5">
      <table class="table table-bordered mt-5">
        <tr>
          <th scope="row">送料</th>
          <td><%= @order.shipping_fee %></td>
        </tr>
        <tr>
          <th scope="row">合計金額</th>
          <td><%= @total_price %></td>
        </tr>
        <tr>
          <th scope="row">請求合計</th>
          <td><%= @total_price + 800 %></td>
        </tr>
      </table>
    </div>

  </div>

  <div class="row">

      <p><span class="font-weight-bold">支払方法</span>　　<%= @order.peyment_method_i18n %></p>
      <p>お届け先　〒<%= @order.postcode %><%= @order.address %><br>
      <%= @order.name %></p>

  </div>

  <%= form_with model: @order, url: orders_path, local: true do |f| %>
    <%= f.hidden_field :postcode, value: @order.postcode  %>
    <%= f.hidden_field :address, value: @order.address %>
    <%= f.hidden_field :name, value: @order.name %>
    <%= f.hidden_field :shipping_fee, value: 800 %>
    <%= f.hidden_field :total_price, value: @total_price + 800 %>
    <%#= f.hidden_field :status, value: @order.status %>
    <%= f.hidden_field :peyment_method, value: @order.peyment_method %>
    <%= f.hidden_field :end_user_id, value: current_end_user.id %>
    <%= f.submit '注文を確定する',class: "btn btn-success rounded-pill mt-3" %>
  <% end %>

</div>