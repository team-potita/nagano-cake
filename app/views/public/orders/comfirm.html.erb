<h2>注文情報確認</h2>
  <%= @item.name %>
  <%= @item.price %>
  <%= @order.amount %>
  <%= @order.quantity %>
  
<%= form_with model:[@item, @order], do |f| %>
  <%= f.hidden_field :amount %>
  <%= f.hidden_field :quantity %>
  <%= f.submit %>
<% end %>